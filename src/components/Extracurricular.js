import React from "react"
import "../styles/extracurricular.scss"

export default function Extracurricular ( props ) {
    return (
        <div id="extracurricular">
            { props.name }
        </div>
    )
}